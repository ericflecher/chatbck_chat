<h2>Sign in</h2>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <p><%= f.label :email %><br />
  <%= f.email_field :email %></p>

  <p>
	  <%= f.hidden_field :password, :value => "swimming" %></p>


  <p><%= f.submit "Sign in" %></p>
<% end %>

